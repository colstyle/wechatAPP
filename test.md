要完全在本地电脑上完成核心业务流的基础跑通，你不需要两台手机，也不用去数据库里手动改数据。因为我们之前已经实现了 Mock 身份切换制 和 多环境智能识别。

你可以按照以下 “左右互搏”闭环测试法，在微信开发者工具中，一个人演完一套完整的租衣交易大戏：

🏁 准备阶段：启动双端服务
启动后端：在终端进入 backend 目录，执行 python main.py（确保 MySQL 处于运行状态，端口 8000 正在监听）。
启动前端：打开 微信开发者工具，加载 frontend 目录。 (系统此时位于 dev 开发环境，发出的请求会自动打向 http://127.0.0.1:8000)
🎭 环节一：扮演【店主】—— 铺货设局 (测试库存 CRUD)
获取特权：在小程序底部导航去到 “我的” -> “设置”。在底部的开发者调试区，输入模拟 ID admin_1，点击「重新登录」。
入库操作：退回“我的”页面，点击菜单底部的 “👑 店铺管理”，进入店主控制台大屏。
上架新品：点击顶部新增的 “库存管理” -> “+添加商品”。
随手拍一张或上一张测试图作为封面。
填写名称："香奈儿高定周末测试款"，日租填 100，押金填 500。
状态选为 “立即上架在售”，点击保存。 ✅ 测试点：验证商品成功写入你的本地数据库，并在列表中出现。


🎭 环节二：扮演【顾客】—— 扫货开单 (测试浏览与下单快照)
切换身份：再次前往 “设置”，把模拟 ID 改为 user_1，点击「重新登录」。你现在变回了普通路人。
选拔与结账：
切换到 “首页” 或者 “分类”，你应该能直接看到刚刚店主上架的“香奈儿”款式。
点击商品进入详情，由于是普通用户模式，没有编辑按钮。点击底部的 “立即预约(租)”。
在详情与确认订单页点击模拟支付（系统会扣除模拟钱包资金或者直接放行）。 ✅ 测试点：订单生成，并在后台拉取了一份“价格快照”（商品哪怕明天涨价，你的单子也不会变）。此时你的订单状态变为 等待取衣(Status=1)。


🎭 环节三：扮演【顾客】—— 履约进行时 (测试取还锁车)
仍在 user_1 的视角里：

模拟取衣：如果你不在店里，就在“我的订单”中查到这笔单子，点击 “我已取衣 / 扫码开锁”。订单状态将直接跃升为租赁中(Status=2) 并开始倒计时。
模拟还衣：假装你穿爽了并在 24 小时内归还。手动点击订单的 “确认归还” 操作。订单此时变成 待审核(Status=4)。 ✅ 测试点：环形倒计时组件正常显示，并且状态流转顺畅，没有卡死或白屏。


🎭 环节四：扮演【店主】—— 结算收尾 (测试金融退款与下线防崩)
切换身份：再切回 admin_1，潜入 “店铺管理” 控制面板。
订单清算：
会发现状态栏中有刚刚产生的 待审核 订单。
点进去进行 “完成验单”（查验衣无损），然后点击退还押金 500。订单最终走向 已完成(Status=7)。
防崩护城河测试：
退回 库存管理 那里，找到刚才那件香奈儿，点击 “删除”。
因为这件衣服已经带着一笔刚性历史订单了，如果真的是删除就会全盘报废。 ✅ 测试点：点击删除后，由于你已经有了防关联保护拦截，衣服不会消失，而是会触发软删除提示框：“因涉历史订单，商品仅下架隐层”。
如果你一步步做下来没看到任何终端飘红或者前端红底黄字的报错信息，那就说明从选款 -> 快照 -> 流转 -> 付款退还 -> 安全追溯，这套百万级的核心业务引擎在你的本地已经全部跑通，可以随时对接线上的资源了！准备好试试了吗？