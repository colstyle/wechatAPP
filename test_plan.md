# 👗 小时光租衣舍 - 核心功能测试计划

本计划旨在验证“小时光租衣舍”小程序在 Phase 1 & 2 阶段开发的核心业务逻辑、权限管控及 AI 客服功能。

***

## 一、 角色与权限定义 (RBAC)

| 角色             | 权限范围                                   | 验证方式                                       |
| :------------- | :------------------------------------- | :----------------------------------------- |
| **用户 (User)**  | 选日期、浏览/搜索、下单（单品/套餐）、取衣/还衣、AI咨询、查看个人订单。 | 默认注册用户即为 User 角色。                          |
| **店主 (Admin)** | 查看全量订单、搜索单号/昵称、一键退押、手动扣费、后台改单（换款）。     | 在数据库 `users` 表中手动将 `role` 字段修改为 `'admin'`。 |

***

## 二、 用户端：核心业务流测试 (User Flow)

### 1. 日期优先筛选测试

- [ ] **强制选定**：进入首页是否强制弹出日期选择？未选日期前商品列表是否不显示数据？
- [ ] **库存联动**：选择日期 A 后，手动在数据库预定某件衣服在日期 A 的库存。返回小程序刷新，该衣服是否已在列表消失？

### 2. 3件69.9元套餐测试

- [ ] **数量校验**：在套餐页选择 0-2 件时，底部的“立即预定”按钮是否禁用？
- [ ] **超量限制**：尝试选择第 4 件时，是否弹出“套餐仅限3件”的提示？
- [ ] **金额计算**：总额是否等于 `69.9 (租金) + 3件衣物押金之和`？

### 3. 24小时租赁计时测试

- [ ] **取衣激活**：点击“我已取衣”后，订单状态是否由“待取衣”变为“租赁中”？
- [ ] **实时倒计时**：详情页是否显示 `23:59:59` 开始的秒级倒计时？
- [ ] **还衣申请**：点击“我已还衣”后，倒计时是否停止？状态是否变为“待审核”？

### 4. AI 智能客服测试

- [ ] **业务理解**：提问“套餐多少钱？”、“怎么取衣服？”、“逾期怎么算？”，AI 是否能基于业务文档准确回答？
- [ ] **模型切换**：修改后端 `settings.py` 的 `AI_SERVICE_TYPE`，验证 DeepSeek 与 Gemini 是否均能正常响应。

***

## 三、 店主端：管理管控流测试 (Admin Flow)

### 1. 订单全量检索

- [ ] **入口显示**：仅管理员可见“我的”页面中的“店铺管理”入口。
- [ ] **状态筛选**：切换“租赁中”、“待审核”等标签，数据过滤是否准确？
- [ ] **关键词搜索**：输入用户昵称或订单号后 4 位，能否精准定位订单？

### 2. 财务结算操作 (核心)

- [ ] **一键退押**：针对“待审核”订单执行退押，订单状态是否变为“已完成”？退款金额是否正确？
- [ ] **手动扣费 (逾期/损毁)**：模拟一个逾期订单，店主输入扣费金额（如 10 元）及理由，最终退款总额是否自动扣除了该部分？
- [ ] **操作留痕**：扣费后的备注信息是否在订单详情中正确显示？

### 3. 后台改单测试

- [ ] **换款逻辑**：针对“待取衣”状态的订单，模拟店主通过管理接口修改其中一件衣物的 ID，验证订单总押金是否随之更新。

***

## 四、 边界异常与安全性测试

- [ ] **并发冲突**：模拟高并发场景下，同一件衣服在同一日期被多个用户尝试下单。
- [ ] **权限越权**：手动拼接 URL 访问管理员页面，系统是否能有效拦截或重定向。
- [ ] **数据库自检**：运行 `python check_db.py` 后，所有核心表（15张）是否均已成功创建。

***

## 🛠️ 测试准备命令清单

1. **环境准备**：
   ```bash
   cd backend
   python check_db.py  # 检查并初始化表结构
   python main.py      # 启动后端
   ```
2. **身份提权 (SQL)**：
   ```sql
   -- 将指定 ID 的用户提升为管理员
   UPDATE users SET role = 'admin' WHERE id = 1;
   ```

***

*版本：Phase 2.0 | 更新日期：2026-04-09*
