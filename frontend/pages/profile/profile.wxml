<!--pages/profile/profile.wxml-->
<view class="page">
  <!-- 用户信息区 -->
  <view class="user-section">
    <view class="user-info" bindtap="handleLogin">
      <image class="user-avatar" src="{{userInfo.avatar || ''}}" mode="aspectFill"></image>
      <view class="user-detail">
        <text class="user-name">{{userInfo.nickname || '点击登录'}}</text>
        <text class="user-id" wx:if="{{userInfo.openid}}">ID: {{userInfo.openid.substring(0, 8)}}</text>
      </view>
      <text class="arrow-icon">></text>
    </view>
  </view>

  <!-- 订单状态栏 -->
  <view class="order-bar">
    <view class="order-title">我的订单</view>
    <view class="order-tabs">
      <view class="order-tab" bindtap="onOrdersTap" data-status="1">
        <text class="order-icon">📦</text>
        <text class="order-text">待取衣</text>
        <text class="order-badge" wx:if="{{orderStats.pendingPickup}}">{{orderStats.pendingPickup}}</text>
      </view>
      <view class="order-tab" bindtap="onOrdersTap" data-status="2">
        <text class="order-icon">👗</text>
        <text class="order-text">租赁中</text>
        <text class="order-badge" wx:if="{{orderStats.renting}}">{{orderStats.renting}}</text>
      </view>
      <view class="order-tab" bindtap="onOrdersTap" data-status="4">
        <text class="order-icon">✅</text>
        <text class="order-text">待审核</text>
        <text class="order-badge" wx:if="{{orderStats.pendingAudit}}">{{orderStats.pendingAudit}}</text>
      </view>
    </view>
  </view>

  <!-- 功能列表 -->
  <view class="function-section">
    <view class="function-group">
      <view class="function-item" bindtap="onServiceTap">
        <view class="item-left">
          <text class="item-icon">🤖</text>
          <text class="item-text">AI 客服</text>
        </view>
        <text class="arrow-icon">></text>
      </view>
    </view>

    <view class="function-group">
      <!-- 店主管理入口 (仅管理员可见) -->
      <view class="function-item" bindtap="onAdminTap" wx:if="{{userInfo.role === 'admin'}}">
        <view class="item-left">
          <text class="item-icon">🏪</text>
          <text class="item-text admin-text">店铺管理</text>
        </view>
        <text class="arrow-icon">></text>
      </view>
    </view>
  </view>

  <!-- 退出登录 -->
  <view class="logout-section" wx:if="{{userInfo.nickname}}">
    <button class="btn-logout" bindtap="onLogout">退出登录</button>
  </view>
</view>
