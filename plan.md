# 青岛单人自助租衣小程序 - 开发执行计划

## 一、 核心业务逻辑重构 (已完成 ✅)
- [x] **数据库模型优化**
    - [x] 增加订单逾期自动计算逻辑支持。
    - [x] 确保 `reservations` 表能准确锁定日期。
- [x] **后端 API 重构 (order.py)**
    - [x] `create_order`: 强制校验 `start_date`，实现 3 件 69.9 元套餐逻辑（租金固定，押金累加）。
    - [x] `pickup_order`: 实现“点击取衣”开始 24 小时倒计时逻辑，计算 `expected_return_time`。
    - [x] `return_order`: 用户端申请归还，等待店主核验。
    - [x] 逾期逻辑：自动统计逾期时长（小时）。
- [x] **后端 API 重构 (product.py)**
    - [x] `get_products`: 强化按日期筛选功能，确保已被预定的衣服在选定日期不可见。

## 二、 前端流程改造 (已完成 ✅)
- [x] **全局日期管理**
    - [x] 在 `app.js` 或首页实现全局日期选择，用户进入必须先选日期。
- [x] **下单流程重构**
    - [x] 单品租赁：选日期 -> 选衣服 -> 下单。
    - [x] 套餐租赁：选日期 -> 选 3 件衣服 -> 校验数量 -> 下单。
- [x] **订单详情页**
    - [x] 增加 24 小时倒计时显示。
    - [x] 增加“我已取衣”和“我已还衣”按钮。

## 三、 AI 智能客服集成 (已完成 ✅)
- [x] **后端 AI 接口 (ai.py)**
    - [x] 集成 Gemini & DeepSeek API，导入业务需求文档作为 System Prompt。
- [x] **前端客服界面**
    - [x] 实现简单的对话框浮窗及全屏对话页。

## 四、 店主后台增强 (已完成 ✅)
- [x] **管理 API (admin.py)**
    - [x] 一键退押金（模拟微信支付退款逻辑）。
    - [x] 手动扣除押金功能（逾期/损坏）。
    - [x] 手动修改订单衣物（换款逻辑）。

## 五、 外部系统对接 (待执行 ⏳)
- [ ] TTLock 通通锁真实 API 对接 (已预留接口)。
- [ ] 微信支付 V3 真实环境对接 (框架已就绪)。
- [x] 店主管理端 UI 界面开发 (已完成)。

## 六、 Phase 2: 店主管理与支付框架 (已完成 ✅)
- [x] **店主管理端 (Admin UI)**
    - [x] 后端：增加 `role` 字段区分管理员，增加全量订单查询 API。
    - [x] 前端：实现管理员专用入口（个人中心展示）。
    - [x] 前端：实现订单管理列表页（支持搜索与状态筛选）。
    - [x] 前端：实现订单详情管理页（集成退押、扣费、换款功能）。
# 青岛单人自助租衣小程序 - 开发执行计划

## 一、 核心业务逻辑重构 (已完成 ✅)
- [x] **数据库模型优化**
    - [x] 增加订单逾期自动计算逻辑支持。
    - [x] 确保 `reservations` 表能准确锁定日期。
- [x] **后端 API 重构 (order.py)**
    - [x] `create_order`: 强制校验 `start_date`，实现 3 件 69.9 元套餐逻辑（租金固定，押金累加）。
    - [x] `pickup_order`: 实现“点击取衣”开始 24 小时倒计时逻辑，计算 `expected_return_time`。
    - [x] `return_order`: 用户端申请归还，等待店主核验。
    - [x] 逾期逻辑：自动统计逾期时长（小时）。
- [x] **后端 API 重构 (product.py)**
    - [x] `get_products`: 强化按日期筛选功能，确保已被预定的衣服在选定日期不可见。

## 二、 前端流程改造 (已完成 ✅)
- [x] **全局日期管理**
    - [x] 在 `app.js` 或首页实现全局日期选择，用户进入必须先选日期。
- [x] **下单流程重构**
    - [x] 单品租赁：选日期 -> 选衣服 -> 下单。
    - [x] 套餐租赁：选日期 -> 选 3 件衣服 -> 校验数量 -> 下单。
- [x] **订单详情页**
    - [x] 增加 24 小时倒计时显示。
    - [x] 增加“我已取衣”和“我已还衣”按钮。

## 三、 AI 智能客服集成 (已完成 ✅)
- [x] **后端 AI 接口 (ai.py)**
    - [x] 集成 Gemini & DeepSeek API，导入业务需求文档作为 System Prompt。
- [x] **前端客服界面**
    - [x] 实现简单的对话框浮窗及全屏对话页。

## 四、 店主后台增强 (已完成 ✅)
- [x] **管理 API (admin.py)**
    - [x] 一键退押金（模拟微信支付退款逻辑）。
    - [x] 手动扣除押金功能（逾期/损坏）。
    - [x] 手动修改订单衣物（换款逻辑）。

## 五、 外部系统对接 (待执行 ⏳)
- [ ] TTLock 通通锁真实 API 对接 (已预留接口)。
- [ ] 微信支付 V3 真实环境对接 (框架已就绪)。
- [x] 店主管理端 UI 界面开发 (已完成)。

## 六、 Phase 2: 店主管理与支付框架 (已完成 ✅)
- [x] **店主管理端 (Admin UI)**
    - [x] 后端：增加 `role` 字段区分管理员，增加全量订单查询 API。
    - [x] 前端：实现管理员专用入口（个人中心展示）。
    - [x] 前端：实现订单管理列表页（支持搜索与状态筛选）。
    - [x] 前端：实现订单详情管理页（集成退押、扣费、换款功能）。
- [x] **微信支付 V3 框架搭建**
    - [x] 后端：封装支付工具类，支持证书加载、签名验证与退款请求封装。
    - [x] 后端：将现有的模拟退款逻辑重构为框架调用模式。
- [ ] **TTLock 接口预留**
    - [ ] 保持现有的 `door_lock_password` 字段与逻辑，暂不进行真实 API 调用。

---
*状态更新日期：2026-04-09*

## 七、 Phase 3: 精简 MVP（核心闭环优先）(已完成 ✅)

### 目标
- 用户端只保留“选日期 -> 选衣 -> 下单 -> 取衣(24h计时) -> 还衣 -> 待审核”的核心闭环。
- 店主端只保留“全量订单 -> 退押/扣费”的核心闭环。
- 其余功能先隐藏入口或停用，降低复杂度，便于测试与迭代。

### 角色与权限
- **用户(User)**：选日期、浏览/搜索衣物、商品详情、单品/套餐下单、我的订单、取衣/还衣、AI客服。
- **店主(Admin, users.role=admin)**：全量订单列表/搜索/筛选、订单详情操作、一键退押、手动扣费。

### 前端精简清单
- [ ] 首页精简：仅保留日期选择、分类入口、按日期商品列表；移除品牌/搭配/推荐文章等复杂模块。
- [ ] 移除“收货地址”相关入口与跳转（保留后端表结构，不在前端暴露）。
- [ ] 商品详情页精简：移除“加入租赁车”，仅保留“立即预定”与客服入口；修复底部按钮溢出。
- [ ] 非核心功能先隐藏入口：收藏、评价、搭配、订阅、预约（代码可保留但不暴露入口）。

### 后端策略
- [ ] TTLock 真实对接暂不做，保留接口与字段不动。
- [ ] 微信支付 V3 暂不接入真实证书，保留支付/退款框架与调用链路。

## 八、 Phase 8: 高奢审美与架构重构 (进行中 🚀)

### 🎨 审美升级 - 从“功能”到“氛围”
- [ ] **视觉系统重塑**：
    - [ ] 弃用默认色系，采用燕麦白/哑光咖啡等**莫兰迪高奢色调**。
    - [ ] 实现全站**沉浸式顶部配置**（Custom Navigation Bar）。
    - [ ] 引入 **Glassmorphism (玻璃拟态)** 悬浮组件与吸底操作栏。
- [ ] **图片体验工程**：
    - [ ] 统一服装展示比例为 **3:4/4:5**，强制懒加载与**微动效骨架屏**。

### 🏗️ 架构优雅化 - 真正的组件化开发
- [ ] **原子组件库建立**：
    - [ ] `cp-product-card`: 封装高性能商品卡片。
    - [ ] `cp-calendar-sheet`: 打造全屏仪式感日历选择，取代原生选择器。
    - [ ] `cp-nav-bar`: 实现可自定义的沉浸式导航。
- [ ] **交互“Aha Moment”**：
    - [ ] 24H 倒计时升级为**环形可视化仪表盘**。
    - [ ] 全屏仪式感开场日历，营造“租赁仪式感”。

    这是一个非常专业且切中要害的需求。

小时光租衣舍 - 高奢审美重构与架构设计计划
本项目将从目前的基础功能形态，进化为具有**现代高奢优雅（Modern Elegant）**风格的品牌化应用。我们将通过建立严谨的组件化架构与视觉规范，确保审美与专业性达到行业顶尖水平。

用户评审要求
IMPORTANT

自定义导航栏切换：开启 navigationStyle: custom 后，所有页面的原生顶部标题栏将消失。我们将通过全局组件 cp-nav-bar 统一接管，这将显著提升视觉沉浸感，但需要确保各页面已适配安全区。

TIP

视觉资产重塑：目前的 Emoji 与原生 Icon 将被替换为 AI 生成的极简主义、高质感线性矢量图标。

方案设计
1. 审美系统 (Design Tokens)
我们将采用“大地莫兰迪”色系，营造温暖、高级、值得信赖的氛围。

类型	变量名	色值	用途
背景	--bg-oatmeal	#FDFCF9	全局页面背景，比纯白更具质感
主色	--color-ebony	#1C1C1C	标题、主要文字、强调色
辅色	--color-gold	#C5A059	价格、主按钮、重要标签（哑光金）
中性	--color-sage	#8E9775	辅助信息、次要按钮（灰绿色）
边框	--border-soft	rgba(28, 28, 28, 0.05)	极其轻微的分隔线
2. 文件夹层级优化
text
frontend/
├── components/           ← 建立组件库
│   ├── cp-nav-bar/       ← 沉浸式导航
│   ├── cp-product-card/  ← 高级商品卡片
│   └── cp-price/         ← 格式化价格组件
├── styles/
│   └── theme.wxss        ← 设计变量定义
├── images/
│   └── tabbar/           ← 重定义的图标
任务列表
Phase 1: 基建与审美底座 (Current)
[MODIFY] 
app.json
设置 "navigationStyle": "custom"。
将 "tabBar" 的 "selectedColor" 修改为哑光金 #C5A059。
[MODIFY] 
app.js
在 onLaunch 中计算并存储 navBarData（包含胶囊按钮位置、状态栏高度），确保自定义导航定位精准。
[NEW] 
theme.wxss
定义全局 CSS 变量、字体层级 (Inter/PingFang SC-Light)。
[MODIFY] 
app.wxss
彻底重构基础样式，实现优雅的卡片布局规范与 Glassmorphism 基础辅助类。
Phase 2: 原子组件库构建
[NEW] 
cp-nav-bar
功能：支持透明渐变、沉浸式背景、自动处理胶囊位。
[NEW] 
cp-product-card
设计：3:4 比例布局，增加 Skeleton 骨架屏占位，价格右下角悬浮设计。
Phase 3: 视觉资产重塑 (Icons)
使用 generate_image 生成以下图标：
首页 (Home): 极简房屋/衣橱线条。
分类 (Category): 优雅的四宫格变体。
订单 (Order): 复古挂衣架或衣物挂钩。
我的 (Profile): 极简人像轮廓。
每个图标分为：未选中（灰绿色线条）、选中（金/黑色实填充或加粗线条）。
验证计划
视觉验证：在真机与模拟器上检查 cp-nav-bar 是否准确避开胶囊按钮。
性能验证：确保 cp-product-card 的大量图片加载时，Skeleton 动效流畅无卡顿。
一致性验证：检查所有页面的 Emoji 是否已被对应的 UI Icon 替换。
待执行确认：请确认是否通过此计划，我将立即开始生成图标并重写底层样式。

### 后端策略
- [ ] 保持 API 返回结构的纯净性，为前端预留 `style_metadata` 类型支持。
