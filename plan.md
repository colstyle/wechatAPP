# 青岛单人自助租衣小程序 - 开发执行计划

## 一、 核心业务逻辑重构 (已完成 ✅)
- [x] **数据库模型优化**
    - [x] 增加订单逾期自动计算逻辑支持。
    - [x] 确保 `reservations` 表能准确锁定日期。
- [x] **后端 API 重构 (order.py)**
    - [x] `create_order`: 强制校验 `start_date`，实现 3 件 69.9 元套餐逻辑（租金固定，押金累加）。
    - [x] `pickup_order`: 实现“点击取衣”开始 24 小时倒计时逻辑，计算 `expected_return_time`。
    - [x] `return_order`: 用户端申请归还，等待店主核验。
    - [x] 逾期逻辑：自动统计逾期时长（小时）。
- [x] **后端 API 重构 (product.py)**
    - [x] `get_products`: 强化按日期筛选功能，确保已被预定的衣服在选定日期不可见。

## 二、 前端流程改造 (已完成 ✅)
- [x] **全局日期管理**
    - [x] 在 `app.js` 或首页实现全局日期选择，用户进入必须先选日期。
- [x] **下单流程重构**
    - [x] 单品租赁：选日期 -> 选衣服 -> 下单。
    - [x] 套餐租赁：选日期 -> 选 3 件衣服 -> 校验数量 -> 下单。
- [x] **订单详情页**
    - [x] 增加 24 小时倒计时显示。
    - [x] 增加“我已取衣”和“我已还衣”按钮。

## 三、 AI 智能客服集成 (已完成 ✅)
- [x] **后端 AI 接口 (ai.py)**
    - [x] 集成 Gemini & DeepSeek API，导入业务需求文档作为 System Prompt。
- [x] **前端客服界面**
    - [x] 实现简单的对话框浮窗及全屏对话页。

## 四、 店主后台增强 (已完成 ✅)
- [x] **管理 API (admin.py)**
    - [x] 一键退押金（模拟微信支付退款逻辑）。
    - [x] 手动扣除押金功能（逾期/损坏）。
    - [x] 手动修改订单衣物（换款逻辑）。

## 五、 外部系统对接 (待执行 ⏳)
- [ ] TTLock 通通锁真实 API 对接。
- [ ] 微信支付 V3 真实环境对接。
- [ ] 店主管理端 UI 界面开发。

---
*状态更新日期：2026-04-09*
